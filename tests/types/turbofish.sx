// Test: Turbofish syntax ::<>

fn identity<T>(x: T) -> T {
    x
}

fn main() -> i64 {
    // Explicit type parameter with turbofish
    let result = identity::<i64>(42);
    
    if result == 42 {
        println("PASS: turbofish syntax works");
    }
    
    0
}
