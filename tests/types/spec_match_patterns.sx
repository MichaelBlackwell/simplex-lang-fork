// Test: Simple match pattern binding

enum Option<T> {
 Some(T),
 None
}

fn main() -> i64 {
 let opt: Option<i64> = Option::Some(100);
 
 let result = match opt {
 Option::Some(val) => val,
 Option::None => 0,
 };
 
 if result == 100 {
 println("PASS: match extracted value");
 }
 
 0
}
