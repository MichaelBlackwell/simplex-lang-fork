// Minimal neural gate test

neural_gate threshold_test(x: f64) -> bool {
 x > 0.5
}

fn main() -> i64 {
 println("Starting neural minimal test");

 // Set inference mode
 neural_set_training_mode(0);

 println("Set inference mode");

 // Test 1: Basic sigmoid function
 let s: f64 = neural_sigmoid(0.0);
 println("Sigmoid called");

 // The test should pass - sigmoid(0) = 0.5
 if s > 0.4 {
 println("Test PASS: sigmoid works");
 } else {
 println("Test FAIL: sigmoid broken");
 }

 println("Test complete");
 0
}
