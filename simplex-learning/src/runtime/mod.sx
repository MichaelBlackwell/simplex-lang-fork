// Runtime module for continuous learning execution
//
// Provides the main learning loop, metrics collection, and checkpointing
// for real-time continuous learning systems.

pub mod learner;
pub mod metrics;
pub mod checkpoint;

pub use learner::{ContinuousLearner, LearnerConfig, LearningEvent};
pub use metrics::{MetricsCollector, LearningMetrics, MetricsSummary};
pub use checkpoint::{CheckpointManager, CheckpointConfig};
