// Safety module: Constraints and bounds for safe learning
//
// Ensures that learning cannot break program correctness.
// Hard constraints are always enforced; soft constraints guide optimization.

pub mod constraints;
pub mod bounds;
pub mod fallback;

pub use constraints::{Constraint, SoftConstraint, HardConstraint, ConstraintViolation};
pub use bounds::{SafetyBounds, GradientBounds, WeightBounds};
pub use fallback::{FallbackStrategy, SafeFallback};
